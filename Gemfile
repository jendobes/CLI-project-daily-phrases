source "https://rubygems.org"
require "nokogiri"
require "open-uri"
require "pry"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in daily_phrase.gemspec
gemspec
